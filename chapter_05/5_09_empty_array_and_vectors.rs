fn main(){

    // Declaring empty arrays
    
    //let _a = [];
    // This is not allowed because the compiler can't determine the type of data

    let _a = [""; 0];

    let _b = vec![true; 0];
    let _c = vec![false; 0];

}
